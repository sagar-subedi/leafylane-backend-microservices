apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.35.0 (9532ceef3)
  labels:
    io.kompose.service: bookstore-telegraf
  name: bookstore-telegraf
spec:
  ports:
    - name: "8125"
      port: 8125
      protocol: UDP
      targetPort: 8125
  selector:
    io.kompose.service: bookstore-telegraf
