apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: leafylane
  labels:
    io.kompose.service: leafylane-grafana-volume
  name: leafylane-grafana-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
